# Tasks: map-suggested-repo-excludes

## 1. Digest honors the merged exclude set

- [x] 1.1 In `src/producer/claudeDigest.ts`, give `listDocumentableFiles` and `buildDigestTree` an optional `excludeGlobs` argument that drops every matched tracked file and counts glob-excluded files in the digest's exclusion report, alongside the existing kind buckets. Tests in `claudeDigest.test.ts`: "Claude producer planning honors the merged exclude set › Glob-excluded tracked files are not planned" (a fixture checkout with tracked files under `dist/` + `excludeGlobs: ["dist/**"]` lists none of them and reports the glob-excluded count) and "An area slice never names a glob-excluded file"
- [x] 1.2 Thread the merged excludes into the planning path in `src/producer/claude.ts`: the map/planner `tracked` list and structure tree are built with `cfg.excludeGlobs` applied, so split-threshold, budget, expected-area-count, and every handout agree on the narrowed set. Tests: extend the existing argv-capture split tests ("The map session is told the documentable count, excluding inert files") so a config carrying `excludeGlobs: ["dist/**"]` yields a digest and "Cover the N documentable files" line that omit the matched files (spec: okf-producer › Claude producer planning honors the merged exclude set)
- [x] 1.3 In `src/repoManager/pipeline.ts`, pass `{ ...cfg, excludeGlobs: effectiveExcludes(cfg, repo) }` into `runIsolatedProducer` exactly as the index step already does (D1). Tests: a pipeline/producer test asserting the merged globs reach the `claude` run's config (spec: the merged exclude set is supplied to the producer)

## 2. Map proposals, gate, and strip

- [x] 2.1 Extend the map artifact schema in `src/producer/claudePlan.ts`: `AreaMap` gains an optional `exclude` list of `{ path, reason }` (directory path, non-empty reason); the zod parse and round-trip accept maps with and without it. Tests: `claudePlan.test.ts` parses a map with `exclude` and one without (spec: okf-producer › Claude producer map exclusion gate)
- [x] 2.2 Implement the pure gate `gateExcludeProposals` in `claudePlan.ts` beside `validateMap`: for each proposal, verify it names one whole non-root repo-relative directory with at least one currently-documentable tracked file (not kind-excluded, not already matched by the merged excludes), and that no tracked file under it is protection-worthy — judged by three deterministic lists (code-file extensions, documentation extensions, configuration filenames/patterns) defined beside the digest's existing kind lists. Normalize a directory proposal to its `<dir>/**` glob. Tests: accept an `*.xcassets` `Contents.json` tree; reject a directory containing a code file, a doc file, a config-named file, the repo root, a malformed path, and a no-op (nothing currently documentable under it) (spec scenarios: A certifiable data group is accepted / matches code / root or malformed / no-op invalidates)
- [x] 2.3 Implement the deterministic strip: given accepted proposals and the narrowed documentable list, drop each area `path` that now owns no documentable file and drop areas left owning none, then re-run the existing repair/split/validate sequence against the narrowed count. Tests in `claudePlan.test.ts`: an area owning only excluded files is dropped, a partly-excluded area keeps only its remaining paths, and the surviving map validates (spec: The map is stripped before area planning)
- [ ] 2.4 Wire the gate and strip into the map path in `src/producer/claude.ts`: after a map session returns and before the existing repair/validate/save, run the gate; if any proposal fails, clear the planning artifacts, note the rejected path and reason, and remap (no partial application); if all pass, narrow tree + `tracked`, strip, revalidate, and `saveMap` with the proposals retained, then let the area loop render slices from the narrowed tree. Tests with session shims: (a) a map carrying one valid proposal proceeds to parts with the excluded files absent from area slices and `excludeGlobsApplied` reported; (b) a map with an invalid proposal is cleared and the remap note names it; (c) a resumed run re-runs the gate on the saved map's proposals before reuse (spec scenarios: applied / invalidates the map / slices from the narrowed set / resumed run re-gates)
- [ ] 2.5 Update the authoring guidance: `MAP.md` and `mapDirectives` describe the exclusion escape hatch — a digest group judged data payload (catalogs, golden/fixture data, asset metadata) may be proposed in the map's `exclude` list instead of covered, naming one whole directory with one line of evidence; mixed or uncertain trees must be covered; a failing proposal invalidates the map, so proposing nothing is the default. Tests: the map directives/prompt assertions cover the new wording (spec: okf-producer › Claude producer map ownership guidance scenarios — escape hatch, mixed/uncertain forbiddance)

## 3. Report and persist accepted globs

- [ ] 3.1 Add `excludeGlobsApplied?: string[]` to `ProducerRun` in `src/producer/contract.ts`, set by `claude` only on an init split run whose gate accepted proposals and whose run completed; `runIsolatedProducer` passes it through unchanged and `openwiki` never sets it. Tests: a claude run reports the accepted globs; an openwiki run and a claude run without proposals report none; the producer-contract suite still passes (nothing under acceptance names a producer)
- [ ] 3.2 Add `appendRepoExcludeGlobs(repoId, globs)` in `src/repoManager/registry.ts`: reload `registry.yaml`, normalize + dedupe (order preserved), append to that repo's `excludeGlobs` only, atomic tmp+rename write, and a shared `mergeGlobs` helper reusable by the index step. Tests: appends and round-trips; a concurrent human edit made before the write survives (reload semantics); every other repo and field is byte-identical; no repo entry other than the target changes (spec: repo-manager › Per-repo exclude globs — Map-run globs persisted; Registry write discipline — the run's only registry write)
- [ ] 3.3 Persist in `src/repoManager/pipeline.ts` between the producer's success/continuity write and `indexRepo`, only when the completed run reports applied globs, and recompute the index step's merged-excludes Config with the applied globs appended so the same run's crawl/purge honors them. Tests: a completed claude run persists the globs and the same run registers no chunks for matched paths ("Map-run globs apply to the same run's index"); a failed/rate-limited run leaves the repo's `excludeGlobs` unchanged ("A failed run leaves the globs unset")

## 4. Close out

- [ ] 4.1 Run the full offline suite `bun test ./src ./test`, `bun run lint`, `bun run format`, `bun run typecheck`, and `openspec validate --specs`, and confirm every spec scenario above has a passing test named "<Requirement> › <Scenario>"
