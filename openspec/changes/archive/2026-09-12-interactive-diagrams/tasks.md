## 1. Interactive diagram viewer

- [x] 1.1 Extend `MERMAID_BOOTSTRAP` in `src/server/wiki.ts` to await `mermaid.run`, then make each rendered diagram interactive: set pan cursor and touch behavior, adjust the `viewBox` for pointer-drag panning, non-passive wheel zoom at the pointer, and 1.2-step Zoom in/Zoom out buttons in a `.diagram-tools` overlay, bounded to 0.2×–5× of the natural viewBox; add the overlay/cursor styles; cover the viewer hooks and controls on a diagram page and confirm no-diagram pages stay script-free in `src/server/wiki.test.ts`; verify with `bun test src/server/wiki.test.ts`.
- [x] 1.2 Run `bun run test`, `bun run lint`, `bun run typecheck`, `bun run format:check`, and `openspec validate --specs`; fix any failures and confirm the new `wiki-viewer` scenario has a passing test.
- [x] 1.3 Add the Expand popup (native `<dialog>`, cloned SVG, own pan/zoom, Close/Escape/backdrop dismissal) and make wheel zoom track the scroll amount, centered via pointer client coordinates, with a 1.15 button step; cover the controls, popup hooks, and zoom math presence in `src/server/wiki.test.ts`.
- [x] 1.4 Run `bun run test`, `bun run lint`, `bun run typecheck`, `bun run format:check`, and `openspec validate --specs`; fix any failures and confirm every `wiki-viewer` scenario added by this change has a passing test.
- [x] 1.5 Rework the viewer to DeepWiki's model: inline diagrams static with click/Enter/Space/Expand opening a 90vw × 90vh centered popup; move pan and wheel into the popup only; set the wheel step to 1.06 per event and buttons to 1.2; hover-reveal the inline Expand control and dim a shared backdrop; cover the five scenarios in `src/server/wiki.test.ts`.
- [x] 1.6 Run `bun run test`, `bun run lint`, `bun run typecheck`, `bun run format:check`, and `openspec validate --specs`; fix any failures and confirm every `wiki-viewer` scenario added by this change has a passing test.
- [x] 1.7 Style the diagram card: a slim always-on border with a hover border emphasis instead of a background fill (the Mermaid node fill equals the hover surface, which made nodes unreadable), and confirm with `bun test src/server/wiki.test.ts` plus format/lint.
