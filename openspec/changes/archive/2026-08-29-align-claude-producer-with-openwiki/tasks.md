## 1. Symlink defense in citation resolution (`src/producer/grounding.ts`)

- [x] 1.1 Replace the private `isFile` and the `lineCountOf` closure with a resolution step that `lstat`s the cited path, treats a symbolic link as unresolved without reading it, and verifies the `realpath` of the file both stays inside the checkout's real root and matches the path named — mirroring openwiki's `resolver.ts:201-223` — counting any failure as unresolved rather than throwing, so one hostile citation degrades the score instead of aborting the bundle's scoring; verify unit tests in `grounding.test.ts`: a plain file still resolves, a symlink to a file outside the checkout is unresolved and its target never read, a symlink to a file inside the checkout is unresolved, a file reached through a symlinked parent directory is unresolved, and the surrounding pages are still scored

## 2. Page plan and durable checkpoint (`src/producer/plan.ts`)

- [x] 2.1 Define the plan and checkpoint shapes with `zod`: a `PlanPage` (`path`, `type`, `title`, `brief`, `sourcePaths`), a `Plan` (`pages`, optional `deletePages`), and a `RunState` (`mode`, `targetSha`, `plan`, and per-page `status: "pending" | "complete"` with an `attempts` count); export `OVERVIEW_PAGE = "overview.md"`; verify unit tests: a well-formed plan parses, a plan missing a required field is rejected, an unknown extra field does not break parsing
- [x] 2.2 Implement `parsePlan(fileContents)`: parse the planner's scratch file — the one channel the session is told to write — returning a discriminated failure rather than throwing; verify unit tests: a well-formed file parses, an absent file, unparseable input and a plan missing a required field each return a failure naming why, and an empty `pages` array parses (the mode decides whether it is legal)
- [x] 2.3 Implement `normalizePlan(plan, {mode, existingPages})`: drop duplicate paths, reject a path naming a `RESERVED_NAMES` entry or escaping the bundle, insert an `overview.md` job when absent, filter `overview.md` out of `deletePages`, and sort so `overview.md` is always last; verify unit tests: duplicates collapse, `index.md`/`log.md` are rejected, a plan without an overview gains one, a `deletePages` naming the overview keeps the page, overview sorts last regardless of input order
- [x] 2.4 Implement `readRunState(bundleDir)` / `writeRunState(bundleDir, state)` against `<bundle>/.odw-run.json`, writing atomically (temp file plus rename) so a kill mid-write cannot corrupt the checkpoint, and returning `null` for an absent or corrupt file rather than throwing; verify unit tests: round-trip, absent file returns null, corrupt JSON returns null, a partial write leaves the previous state readable
- [x] 2.5 Implement `resumableJobs(state, {mode, targetSha})`: return the pending jobs when the recorded mode and target commit match, and signal "replan" when either differs; verify unit tests: matching state resumes at pending jobs only, a changed `targetSha` signals replan, a changed mode signals replan, a state with every job complete yields no jobs

## 3. Phase prompts (`src/producer/skill/skills/okf-wiki/`)

- [x] 3.1 Trim `SKILL.md` to the contract every session shares — hard rules, `sources` citations, cross-linking, diagrams, page structure and prose, guardrails — removing the `## Modes` section, the "write `index.md` first… it is your checklist" step that `finalize.ts` contradicts, and the `log.md` instruction (`log.md` is in `RESERVED_NAMES` and excluded everywhere); verify `claude.test.ts` assertions that the shared contract still carries every existing style rule the spec requires (related-pages section, bold-on-first-mention, `tags`, quoted Mermaid labels, line-range citations, plain prose) and no longer instructs writing `index.md` first
- [x] 3.2 Write `PLANNER.md`: the staged exploration protocol (map manifests, entry points and public surfaces → trace representative end-to-end control and data flow → verify boundaries and invariants against focused tests and neighbouring implementations), the "organize around owned systems, runtime domains and cross-system workflows rather than mirroring the source tree" principle with the `architecture/` `concepts/` `workflows/` `operations/` `integrations/` `testing/` taxonomy, and the plan JSON contract naming the `PLAN_FILE:` destination; verify `claude.test.ts` assertions for each instruction the spec's planner scenarios name
- [x] 3.3 Write `PAGE.md`: the page content checklist, the "do not turn the page into a source-file inventory" anti-pattern, interlinking discipline (relation verbs, the two-connection floor or an explicit standalone statement, no padding and no reflexive reciprocal links), `description` written for retrieval, and concise defined as dense-not-short with page count and length ruled out as targets; verify `claude.test.ts` assertions for each instruction the spec's page-depth scenarios name
- [x] 3.4 Implement prompt composition in `claude.ts`: `authoringPrompt()` returns the shared contract, and a phase prompt is `SKILL.md` plus one phase file with frontmatter stripped; verify a unit test that a planner session's system prompt contains both the shared contract and the planner protocol, and a page session's contains the shared contract and the page checklist but not the planner protocol

## 4. Per-session timeout knob (`src/config/config.ts`)

- [x] 4.1 Add `ODW_CLAUDE_STEP_TIMEOUT_SEC` (`z.coerce.number().int().positive().default(900)`) threaded to `cfg.claude.stepTimeoutSec`, and document it in `.env.template` alongside `ODW_CLAUDE_TIMEOUT_SEC` with a line stating that the latter is now the whole-run budget; verify a `config.test.ts` assertion on the default and on an override

## 5. Orchestrator (`src/producer/claude.ts`)

- [x] 5.1 Factor the existing spawn/classify body into an internal `runSession(cfg, {systemPromptFile, prompt, timeoutMs, cwd, env})` returning the current classified shape, leaving `classifyResult`, `checkClaude`, `suspectsRateLimit`, `ALLOWED_TOOLS` and `DENIED_TOOLS` unchanged; verify the existing `claude.test.ts` classification and spawn-failure tests still pass against the extracted helper
- [x] 5.2 Replace `inputDirectives` with `plannerDirectives(mode, input, planFile)` and `pageDirectives(job, pageMap, input)`, each emitting its machine-readable directive line (`PLAN_FILE:` / `PAGE_PATH:`) plus the mode, change set, `type` vocabulary and repair context the phase needs; verify unit tests: an update planner prompt names the changed paths and the existing `type` vocabulary, a page prompt names exactly one `PAGE_PATH` and lists the other planned pages for cross-linking
- [x] 5.3 Make every `claude*` shim in `test/helpers/shim.ts` phase-aware by branching on `PLAN_FILE:`/`PAGE_PATH:` in argv, and add `claudePipelineHappy(bundleDir)` (emits a plan covering a fixture bundle, then writes each page on its own invocation) and `claudeStallsOnOnePage(bundleDir, page)` (one page's session hangs, the rest succeed); verify the existing shim-driven tests in `producer.test.ts` and `producerPipeline.test.ts` pass unchanged against the new shims
- [x] 5.4 Rewrite `runClaude` as the orchestrator: read the checkpoint, replan on drift, run the planning session when no valid plan is checkpointed, then one session per pending job in plan order, persisting the checkpoint after every transition and verifying each produced page has frontmatter and a non-empty body before marking it complete; a `rate_limited` session stops the run and reports `rate_limited`, a failed or timed-out session leaves its job pending and moves to the next, and the overall budget stops further sessions; keep `finalizeClaudeBundle` as this file's single call site, running only when every job is complete; verify `claude.test.ts` tests for the spec's invocation and checkpoint scenarios: one session per page, a per-session timeout leaves the run attempting later pages, a resumed run launches no session for a completed page, a moved `targetSha` triggers a fresh planning session, an unparseable plan fails the run with no page written, an empty plan fails the run, a plan naming a reserved file is rejected
- [x] 5.5 Verify the checkpoint is invisible downstream: add assertions that a bundle carrying `.odw-run.json` passes `verifyBundle` unchanged, contributes nothing to `scoreGrounding` or `conceptPages`, is not returned by `walkMd`, and is not indexed — reusing the existing fixtures rather than adding a bundle fixture

## 6. Resumable partial runs (`src/producer/adapter.ts`, `src/producer/isolation.ts`)

- [x] 6.1 Add `partial?: boolean` to `ProducerRun` documenting that it means "resumable work exists", set it from `runClaude` when jobs remain pending, and leave `runOpenwiki` never setting it; verify a unit test that an openwiki run reports no partial and a claude run with pending jobs does
- [x] 6.2 Widen `isolation.ts`'s work-in-progress preserve branch to `run.outcome === "rate_limited" || run.partial === true`, keeping the same pin (`resumeTarget ?? input.targetSha`), the same restore, and no repair retry; verify `producerPipeline.test.ts` tests for the spec's resumable scenarios: a stalled page preserves the work-in-progress area and bumps `attempts`, a spawn error preserves nothing and leaves `attempts` unchanged, the next run resumes and regenerates no completed page, and exhausting `cfg.maxResumeAttempts` still discards the work and surfaces the repo
- [x] 6.3 Verify `contract.test.ts` still passes: `plan.ts` introduces no `producerId ===` comparison and no `ProducerId` import under acceptance, and `finalizeClaudeBundle`/`syncIndexes`/`degradeInvalidMermaidFences` are still imported only by `claude.ts`

## 7. Guaranteed overview page

- [x] 7.1 Enforce the overview page in the orchestrator: its job always runs last with the finished page list in its prompt, an init run cannot report `ok` without `overview.md` present, an update run whose plan neither adds nor removes a page skips regenerating it, and an update run against a bundle missing it adds the job; verify `claude.test.ts` and `producerPipeline.test.ts` tests for each of the spec's overview scenarios, including that `overview.md` is byte-identical after an update that adds no page and that `syncIndexes` leaves it untouched

## 8. Integration, verification, and docs

- [x] 8.1 Run `bun test --timeout 20000 ./src ./test`, `bun run lint`, `bun run format:check`, `bun run typecheck`, and `openspec validate align-claude-producer-with-openwiki --strict`; all green
- [x] 8.2 Extend the gated live spike (`test/spike/`, `ODW_SPIKE=1`) to assert the planning session writes its plan file and that each planned page is produced by its own session, so the orchestration is confirmed against the real CLI and not only against shims
- [x] 8.3 Update `AGENTS.md`'s producer sections and `src/producer/CONTRACT.md` to describe the orchestrated `claude` run, the `.odw-run.json` checkpoint, the `partial` field's role in preserving work in progress, the new `ODW_CLAUDE_STEP_TIMEOUT_SEC` knob, and the guaranteed `overview.md`; record in `src/producer/claude-openwiki-gap.md` which gaps this change closes
