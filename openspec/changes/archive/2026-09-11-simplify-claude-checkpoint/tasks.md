## 1. Plan-file checkpoint (`src/producer/claudePlan.ts`)

- [x] 1.1 Rewrite the module around the plan file: `PLAN_FILE_NAME = ".odw-plan.json"`; a `PlanFile` zod schema of `{ pages, deletePages, appliedAtSha? }` where `pages[].path` is the only required entry field and `type`/`title`/`brief`/`sourcePaths`/`relatedPages` are optional cargo (`.default` / `.optional`, never `.min(1)`-enforced); delete `RunState`, `PageJob`, `RUN_STATE_VERSION`, `initialRunState`, `writeRunState`, `readRunState`, `clearRunState`, and `resumeFrom`. Keep `normalizePagePath` and the `normalizePlan` overview machinery (insert on init or when missing, refuse as a deletion, sort last) — the overview requirement is unchanged. Verify `claudePlan.test.ts`: a well-formed plan parses, entries missing cargo fields parse, a corrupt file reads as absent, reserved/escaping/duplicate paths are rejected or dropped exactly as today.
- [x] 1.2 Implement `loadPlan(bundle, targetSha)`: absent, corrupt, or stamp-mismatched plans return a replan signal naming why; otherwise the plan with an `applied` flag (`appliedAtSha` present). Implement `stampPlan(bundle, plan, targetSha)` writing `{...plan, appliedAtSha: targetSha}`. Verify unit tests: matching stamp resumes, moved commit replans, unstamped plan returns `applied: false`, corrupt JSON replans rather than throwing.

## 2. Orchestrator (`src/producer/claude.ts`)

- [x] 2.1 Point the planner at the bundle: `plannerDirectives` emits `PLAN_FILE:` naming the absolute path inside the bundle (`<checkout>/openwiki/.odw-plan.json`), and the tmpdir staging drops the scratch plan path. After the planning session, read and validate the plan from the bundle; an absent/unparseable/invalid plan fails the run before any page session, exactly as today. Verify the existing planner scenarios still pass: plan precedes generation, unparseable plan fails without pages, empty plan fails, reserved name rejected.
- [x] 2.2 One shared apply-then-produce path for fresh runs and resumes: `loadPlan`; on replan run the planning session (2.1); on `applied: false` delete every planned page and every `deletePages` entry from the bundle (`rm -f`, idempotent), then `stampPlan`; then the page loop. Verify `"Plan-file checkpoint › Plan application is recorded before any page is produced"`: a bundle carrying an unstamped plan plus a stale pre-existing page the plan names has that page deleted before any page session, so the stale content is never kept.
- [x] 2.3 The page loop skips pages where `pageIsWritten` already holds, and a failed or timed-out page session ends with `rm` of its page file — delete `rollbackPage` and the per-page pre-read; there are no per-page checkpoint writes. Verify `"Plan-file checkpoint › A resumed run produces only what is missing"`: against a stamped plan with some pages present, no session is launched for a present page (assert via the shim's `ODW_PAGES_OUT` log) and no planning session runs; and `"Completed pages survive an interrupted run"`: a run stalled by the per-session timeout reports `partial`, and the next run produces only the missing pages.
- [x] 2.4 Remove the plan file before reporting `ok` (both the normal FINISH path and the repair-retry path), so no published bundle or snapshot carries one. Verify `"Plan-file checkpoint › A successful run leaves no plan file behind"` and `"A moved target commit discards the plan"`: a plan stamped with a different commit triggers a fresh planning session.
- [x] 2.5 Keep the invisibility guarantees with the new filename: a bundle carrying `.odw-plan.json` passes `verifyBundle` unchanged, contributes nothing to `scoreGrounding` or `conceptPages`, is not returned by `walkMd`, and is not indexed (`"Plan-file checkpoint › Plan file is invisible to the bundle's consumers"`).

## 3. Shims and pipeline tests (`test/helpers/shim.ts`, `src/repoManager/producerPipeline.test.ts`)

- [x] 3.1 Verify the shims need no structural change: `claudeSessions`' planner branch already writes its plan JSON to `$PLAN_FILE` and creates `./openwiki` first, so the in-bundle plan path rides the bundle through WIP preservation unchanged; the `PLAN_FILE:`/`PAGE_PATH:` directive branching the shims rely on is kept. Verify the existing shim-driven suites (`producer.test.ts`, `producerPipeline.test.ts`, `claude.test.ts`) pass.
- [x] 3.2 Pin the two boundary invariants from design D6 with tests: `stageWip` preserves a `.odw-plan.json` inside a staged WIP bundle, and a failed run's restore leaves no plan file at the published path. Verify the existing resumability scenarios still pass unchanged: preserved-on-partial pinned to the commit being built, spawn error preserves nothing, producer change discards, attempts bounded.

## 4. Docs and validation

- [x] 4.1 Update `AGENTS.md` (producer contract section: `.odw-run.json` → the plan-file checkpoint, the tightened sandbox sentence) and `src/producer/CONTRACT.md` (the checkpoint bullet: plan file as checkpoint, apply-stamp, removed before success). Verify `contract.test.ts` passes unchanged — no `ProducerId` import or producer branch appears under acceptance, and selection still lives only in `run.ts`.
- [x] 4.2 Run `bun test ./src ./test`, `bun run lint`, `bun run format:check`, `bun run typecheck`, and `openspec validate simplify-claude-checkpoint --strict`; all green.
