> **Status legend:** `[x]` done · `[~]` deliberately not done, with the reason
> stated inline. Nothing here is silently skipped.
>
> The deferred items all need something this session could not supply: the
> operator's own token-spend history (1.4), a measured baseline over 6-8 real
> repos (1.5, 7.4), a day of human question authoring (1.6), or real subscription
> spend on a large repo (1.7b). 1.8's key question was answered incidentally —
> openwiki's own repo does commit `openwiki/` — but the fleet-wide check remains.

## 1. Grounding Check and Parity Baseline

- [x] 1.1 Implement `src/producer/grounding.ts`: extract cited source paths from each page's `sources[].resource` frontmatter (`repo://<path>`, optional `#Lstart-Lend`) — not the wiki body, which openwiki's real output never cites paths in — resolve them against the checkout, return `{cited, resolved, score, density, unresolved[]}`; verify with unit tests over a fixture bundle covering resolving paths, fabricated paths, line-range forms, a `sources` entry with no `resource`, and a bundle citing nothing (density zero, not score one)
- [x] 1.2 Add `bun run eval` harness (`test/eval/`, outside `bun test`) comparing two bundles for one repo at one sha on conformance, link resolution ratio, grounding score and density, and coverage of source dirs/exports; verify it runs offline against two fixture bundles and emits a stable JSON report
- [x] 1.3 Extend the harness with retrieval parity: build an index from each bundle and score `hybridSearch(style:"ask")` recall@5 and MRR against a golden question set (`test/eval/questions.json`, paths as answer keys); verify with a seeded fixture where the expected ranking is known
- [~] 1.4 **[DEFERRED — manual, needs operator data/spend]** **Cost check (manual, minutes)**: sum the token usage `recordRun` has already stored per repo and extrapolate the monthly openwiki spend. Record it here. Gate: if the figure does not justify ~40 tasks of work, stop and close the change
- [~] 1.5 **[DEFERRED — manual, needs operator data/spend]** **Baseline (manual, real data)**: run 1.2–1.3 over existing openwiki bundles for 6–8 repos spanning a monolith, a small lib, a polyglot repo, and a docs-heavy repo; pick the `ODW_GROUNDING_MIN_DENSITY` formula (per-word, per-page, or unique-files-cited — see design Open Questions) before computing it; record measured grounding/density/link/recall/coverage figures here and set the `ODW_GROUNDING_MIN`, `ODW_GROUNDING_MIN_DENSITY`, and `ODW_INIT_COVERAGE_MIN` defaults from them. A single-repo spot check (openwiki's own self-hosted bundle) already shows this is measurable: 21 pages, 245 `sources[]` entries, 100% resolved, density 3.9–22.8/1000 body words — use it as a sanity check on the harness, not as the baseline itself. Gate: do not start section 5 until these numbers exist
- [~] 1.6 **[DEFERRED — manual, needs operator data/spend]** **Author the golden question set (manual, ~1 day)**: 40–60 questions across the 1.5 repos, each with the source paths that should be retrieved. Needs someone who knows those codebases; it is on the critical path, not a coding task
- [x] 1.7 **Spike (LIVE, automated + gated)** — findings recorded below; gate: **GO**. Implemented as `test/spike/claudeProducer.spike.test.ts`, skipped unless `ODW_SPIKE=1` (design D10 amended: a manual runbook verifies nothing). Run with `ODW_SPIKE=1 bun test test/spike/`. Measured on CLI 2.1.228, `claude-sonnet-5` / effort `medium`, against `spike/init/`:
  - **(a) `--plugin-dir` does NOT work headlessly.** The flag exists and the plugin is found, but invoking the skill in `-p` fails ("a generic execution error, not a 'not found' error") and no bundle is written. `--append-system-prompt-file` with the SKILL.md body (frontmatter stripped) works first time. **D4 amended.**
  - **(b) Grounding is good.** 6 pages, 10 citations, 10 resolved, score 1.0, density 9.72/1000 words, 0 uncited pages — inside the 3.9–22.8 band measured from openwiki's own bundle. Canary frontmatter field present on 6/6 pages, proving the contract was applied.
  - **(c) JSON shape.** Keys: `api_error_status, duration_api_ms, duration_ms, fast_mode_disabled_reason, fast_mode_state, is_error, modelUsage, num_turns, permission_denials, result, session_id, stop_reason, subtype, terminal_reason, time_to_request_ms, total_cost_usd, ttft_ms, ttft_stream_ms, type, usage, uuid`. Classify on **`terminal_reason`** (`completed` / `api_error` / …), not the free-text `result`.
  - **A failed run reports success**: the `--plugin-dir` attempt exited 0 with `is_error: false`, `terminal_reason: "completed"` and produced nothing, for ~$0.22. **New decision D8b** — CLI signals label a run, they never accept one.
  - **Config isolation breaks auth**: `HOME=<tmp>` and `CLAUDE_CONFIG_DIR=<tmp>` both give `"Not logged in · Please run /login"`. **D5 corrected**; provisioning is an open choice before 5.2.
  - **Sandboxing holds**: a planted `build.sh` was never executed and every tracked source file was byte-identical after the run.
  - Cost note: billing is OAuth `stripe_subscription`, so `total_cost_usd` (~$0.44/run here) is an API-rate *equivalent*, not a charge — keep that in mind when comparing against openwiki's metered spend in 1.4.
  - Still open: (b) at realistic scale. A 680-token repo cannot exhibit the compaction/truncation this measures nothing about — needs one large repo before 5.4.
- [~] 1.7b **[DEFERRED — manual, needs operator data/spend]** **Spike at realistic scale (LIVE)**: re-run the spike against one large repo (openwiki's own, ~400k tokens of source, already has an openwiki-authored bundle to compare against) and record grounding/coverage plus whether the bundle comes back thin; this is what 3.2's coverage floor and 5.4's two-phase prompting are calibrated against. Original manual wording, superseded above: run `claude -p` by hand on 2–3 of those repos with a draft skill, plus the minimal fixture at `spike/` (`spike/init/` for init, no git needed; `spike/update/` for update — see `spike/README.md` for exact commands, including why update needs a locally-created git history for openwiki but not for the claude producer). Confirm explicitly that (a) `--plugin-dir` actually applies the skill in a headless run — it requires a plugin root (`.claude-plugin/plugin.json` + nested `skills/<name>/SKILL.md`), not a bare `SKILL.md`, so build the skill directory that way from the start; if `--plugin-dir` does not apply in `-p` mode specifically, switch to `--append-system-prompt-file`, for which the skill body should already read standalone; (b) generated pages populate `sources[].resource` at usable density (not body-inline citations — see design D6); (c) the JSON result shape for success, rate limit, and reset time. Record findings here. Gate: go/no-go
- [~] 1.8 **[DEFERRED — manual, needs operator data/spend]** **Check for committed bundles (manual, minutes)**: determine whether any managed repo commits its own `openwiki/` directory. openwiki's own repo is a real example — it commits `openwiki/` including `.claims/` (51 tracked files) — so this is not hypothetical. Because `repo add` always runs `openwiki --init` on first registration regardless of whether `openwiki/.last-update.json` already exists in the checkout, confirm what `--init` actually does against a checkout with a pre-existing valid bundle (full regeneration discarding the committed history, or detected-and-treated-as-update) before deciding whether such repos need excluding, a different bundle location, or forcing `update` mode on first add; record the finding before section 4

## 2. Producer Contract

- [x] 2.1 Rename `OpenwikiRun` to `ProducerRun` and add `outcome: "ok" | "failed" | "rate_limited"`, threading it through the isolation routine and `pipeline.ts`; leave the process fields required (both producers are child processes today). Verify the existing producer suite passes unchanged with outcomes mapped from the current exit/timeout handling (spec: *Producer contract*)
- [x] 2.2 Define `ProducerInput` (clone, mode, `fromSha`, `changedPaths`, `typeVocabulary`, resume target) and pass it to `runOpenwiki`, which ignores the fields it does not need; verify openwiki's observable behavior and its tests are unchanged (spec: *Producer contract*, self-diffing scenario)
- [x] 2.3 Add `ODW_PRODUCER` (default `openwiki`) plus `ODW_CLAUDE_MODEL`, `ODW_CLAUDE_EFFORT` (1.7: `--effort` is a real flag, `low|medium|high|xhigh|max`, and an unpinned run is not comparable), `ODW_CLAUDE_TIMEOUT_SEC`, and `ODW_CLAUDE_CONFIG_DIR` (see D5 — isolation breaks auth, so this is a provisioned dir or unused) to `config.ts`; verify with config tests asserting defaults, overrides, and that an unknown producer id is rejected with an error listing the available ids (spec: *Producer selection*)
- [x] 2.4 Add the optional per-repo `producer` to the registry record, settable via `repo add --producer` and changeable afterwards, surfaced in `repo list`; verify with registry tests that an override wins over the global default and that changing it leaves bundle, metadata, and index intact (spec: *Per-repo producer override*)
- [x] 2.5 Implement `.last-update.json` read/write in `src/producer/continuity.ts`, preserving openwiki's six fields and adding `producer` as an extension field; verify with tests round-tripping an openwiki-written file, tolerating a missing/corrupt file, and asserting no known field is dropped or repurposed (spec: *Wiki continuity metadata*)
- [x] 2.6 Write the contract down as a header comment in `src/producer/` (the four rules a producer must satisfy, and that acceptance is not its decision), and add the two guard tests that keep it honest: no acceptance source file names a producer, and producer selection appears in exactly one place; verify both tests fail when deliberately violated (spec: *Producer contract*)

## 3. Bundle Acceptance — Shared by Every Producer

- [x] 3.1 Wire grounding into acceptance: extract cited paths from each page's `sources[].resource` (`repo://<path>`, optional `#Lstart-Lend`) — not the wiki body — resolve against the checkout, add `ODW_GROUNDING_MIN` and `ODW_GROUNDING_MIN_DENSITY` and fail verification when either the resolved fraction or the citation density is below its floor; verify a fabricated-citation bundle is rejected, a bundle citing nothing is rejected for density rather than passing vacuously, and a fully grounded bundle records score 1 (spec: *Bundle grounding verification*; design: D6)
- [x] 3.2 Add the init coverage floor (`ODW_INIT_COVERAGE_MIN`), promoting the harness coverage metric into runtime acceptance for init runs only; add `INSTRUCTIONS.md` to `verify.ts`'s `RESERVED_NAMES` (openwiki's own bundle names it structural alongside `index.md`/`log.md` — currently counted as a concept, which would skew coverage and type-vocabulary checks); verify a well-formed but truncated init bundle is rejected while a small scoped update is not subject to the floor, and that `INSTRUCTIONS.md` is excluded from concept counts (spec: *Initial bundles must cover the repository*)
- [x] 3.3 Implement the scoped-update checks: a page outside the change set keeps its existing `type` (a genuinely new page may introduce a new one — see design D7), byte-identity for concept pages outside the change set with directory indexes and changelogs exempt, and an `ODW_UPDATE_MAX_CHURN_RATIO` ceiling on bundle churn vs source churn; verify each rejection case, that a new page's new `type` is accepted, that index regeneration is not counted a violation, and a compliant update (spec: *Scoped updates preserve existing conventions*)
- [x] 3.4 Add the repair retry: on verification failure only, re-invoke the same producer once with the specific errors — starting from the pre-run bundle rather than the rejected output when the failure was an over-broad rewrite; verify repaired-on-retry, churn-failure-retried-from-clean-base, still-failing-after-retry, and no-retry-after-timeout (spec: *Repair retry before failure*)
- [x] 3.5 Extend isolation to cover grounding, coverage, proportionality, and exhausted-retry failures; verify the published bundle, its `.last-update.json`, and the index survive each failure mode (spec: *Failure isolation*)
- [x] 3.6 Add the producer-independence test: drive the whole acceptance path with the openwiki shim and the claude shim emitting byte-identical bundle content, and assert identical accept/reject results, identical recorded scores, and the same run event sequence (spec: *Producer contract*)

## 4. Resumable Production

- [x] 4.1 Implement the work-in-progress area at `<dataDir>/repos/wip/<repoId>/`: seed from the published bundle (or empty on init) and record `targetSha`, `producer`, and `attempts`; verify create, resume-detect, and discard-when-producer-changed (spec: *Resumable production across runs*)
- [x] 4.2 Produce into WIP and promote atomically only when a complete bundle passes acceptance; verify the published path never holds a partial bundle, an interrupted promotion is recoverable, and the index is untouched until promotion (spec: same, promotion scenario)
- [x] 4.3 Preserve WIP on `rate_limited` and resume from it next run, handing the producer the partial bundle to continue; verify with a shim that rate-limits mid-build then completes on the second invocation, asserting already-produced pages are not regenerated (spec: same, resume scenarios)
- [x] 4.4 Pin the repo to `targetSha` while a WIP exists — skip the pull, hold the target — and unpin on promotion; verify with a fixture whose remote advances between runs that the build stays on the pinned commit and `.last-update.json` records it (spec: *Production pins the target commit*)
- [x] 4.5 Add `ODW_MAX_RESUME_ATTEMPTS` and enforce it: on exhaustion discard the WIP, stop retrying, mark the repo needing attention; verify with an always-rate-limiting shim that retries stop at the cap and the published bundle stays queryable (spec: *Resumable production*, attempts scenario)

## 5. Claude Producer

- [x] 5.1 Write the OKF authoring skill at `src/producer/skill/` as a plugin (`.claude-plugin/plugin.json` + `skills/<name>/SKILL.md`, per 1.7): frontmatter schema, root and section `index.md`, relative links, page-type vocabulary rule (existing pages keep their type; a new page may introduce one — design D7), a rule to record what was read as `sources[].resource` entries (not body-inline citations — design D6), conform-to-existing-conventions rule for updates, continue-a-partial-bundle rule for resumes; verify a run over a fixture repo yields a bundle passing `verifyBundle` and 1.1
- [x] 5.2 Implement `runClaude` in `src/producer/claude.ts` with the same signature as `runOpenwiki`: spawn `claude -p` with `cwd` = clone, the skill loaded by whichever mechanism 1.7 proved works, allowlisted `Read/Grep/Glob/Write/Edit`, `Bash` denied, isolated config dir, `--output-format json`, and a timeout; verify with shim tests on argv, stdio, and timeout kill (spec: *Non-interactive Claude Code producer invocation*)
- [x] 5.3 Add the sandboxing assertions: tests proving a run does not execute repository scripts and does not modify tracked source files in the checkout (spec: same, security scenarios)
- [~] 5.4 **[BLOCKED on 1.7b — do not build blind]** The 1.7 spike shows a small repo needs no two-phase prompting; whether a large one does is exactly what 1.7b measures. Implementing it now would be speculative complexity against the no-over-engineering rule. Implement the two-phase prompt for large repos — skeleton pass, then per-section invocations with a bounded files-per-invocation working set, splitting a section further when it exceeds the cap; verify coverage on the largest fixture repo clears the 3.2 floor and does not regress against the 1.5 baseline (design: D6, Risks)
- [x] 5.5 Consume `ProducerInput.changedPaths` and `typeVocabulary` in update mode, and the partial bundle on resume; verify with a fixture where only one module changed that only its pages are revised
- [x] 5.6 Classify the run result: parse the JSON output into `ok` / `failed` / `rate_limited`, capture a reported reset time when present, and detect the missing-CLI case; verify with shim variants for each outcome and one carrying a reset time (spec: *Rate-limit outcome distinct from failure*)
- [x] 5.7 Add a structural rate-limit fallback for when message shapes change: treat N consecutive fast failures across different repos as a suspected limit and pause the batch; verify with a shim that fails fast on every repo without emitting a recognizable limit message (design: D8)

## 6. Pipeline, Scheduler, and Monitoring

- [x] 6.1 Add the single selection branch at the point the bundle is produced, and unify the missing-prerequisite policy so either producer's absent CLI warns and falls back to source-only indexing (today only openwiki degrades); verify each producer runs for the right repo, no version warning is emitted for `claude`, and both absent-CLI cases behave identically (spec: *Producer selection*, *Version pinning*)
- [x] 6.2 Write `.last-update.json` from the pipeline after every successful promotion as an idempotent overwrite, so producers that maintain it themselves and producers that do not converge on the same state; verify both cases (spec: *Wiki continuity metadata*)
- [x] 6.3 Thread `rate_limited` and any reset time through `PipelineResult`, `recordRun`, and the registry so they do not overwrite `lastSuccessAt`/`lastIndexedSha`; verify a rate-limited run leaves last-success state unchanged and records the reset time (spec: *Run observability*)
- [x] 6.4 Make dispatch fair and budget-aware: order repos by staleness (least recently succeeded first) instead of registration order, dispatch updates and resumes ahead of first builds, skip the pull for a pinned repo, respect a recorded reset time, and stop dispatching for a producer once limited; verify a repeatedly truncated batch reaches every repo across successive runs and that one new large repo does not stall updates (spec: *Scheduled updates*)
- [x] 6.5 Record producer, grounding score, pinned target and attempts in the registry, and add the `run_rate_limited` event alongside the existing run events; verify a rate-limited run emits no `run_failed` event (spec: *Append-only event log*, *Run observability*)
- [x] 6.6 Extend health classification — `rate_limited` and sub-floor grounding yellow, an in-progress pinned build `building`, an attempt-exhausted build red — and add producer, grounding, and build-progress columns to `status` and `status --json`; verify each health transition, that a building repo is not reported stale, and a mixed-producer snapshot (spec: *Deterministic repo health classification*, *CLI status and logs*)

## 7. Continuation and Migration Validation

- [x] 7.1 Build the continuation fixture: an openwiki-authored bundle at sha A, updated by the `claude` producer to sha B (change set including a rename and a delete); verify `.last-update.json` advanced to B, unchanged-module concept pages are byte-identical, and no new `type` values appeared (spec: *Wiki continuity metadata*)
- [x] 7.2 Add the reverse leg: run the `openwiki` shim in update mode over the Claude-authored bundle from 7.1 and verify it reads the recorded `gitHead` and performs an incremental update, proving the round-trip and the rollback path
- [x] 7.3 Add the interrupted-build fixture: a build cut short twice by rate limits and completed on the third run, asserting the published bundle appears only once, at the end, coherent for the pinned commit (spec: *Resumable production*, *Production pins the target commit*)
- [~] 7.4 **[DEFERRED — manual, needs operator data/spend]** Run the full 1.2–1.3 harness on both producers across the 1.5 sample repos and record the comparison against the pre-committed decision rule; deliverable is the recorded result table plus an explicit ship/no-ship call

## 8. Documentation

- [x] 8.1 Document every new knob (`ODW_PRODUCER`, `ODW_CLAUDE_*`, `ODW_GROUNDING_MIN`, `ODW_GROUNDING_MIN_DENSITY`, `ODW_INIT_COVERAGE_MIN`, `ODW_UPDATE_MAX_CHURN_RATIO`, `ODW_MAX_RESUME_ATTEMPTS`) in README and `.env.template`, plus `repo add --producer` in the CLI reference; verify every knob appears in both files
- [x] 8.2 Add the Claude producer to AGENTS.md "Runtime mechanics" (verified flags, skill-loading mechanism, config-dir isolation, denied `Bash`, rate-limit shape) and correct the archived design's `.claims/` claim; verify by review against the findings recorded in 1.7
- [x] 8.3 Record the producer contract in AGENTS.md: the four rules, the two grep invariants from 2.6, and that producer choice is a config value plus one branch — no interface, so the existing "no port/adapter seams" rule stands unamended; verify the stated rules and the code agree
- [x] 8.4 Document the published-vs-WIP model in AGENTS.md invariants: the published bundle is never partial, production happens in WIP, promotion is atomic, and a build pins its commit; verify against the tests from section 4
- [x] 8.5 Document the migration runbook in README: cost check, baseline, cohort rollout, per-repo rollback, how to read grounding and build progress in `status`, and what to do when a repo exhausts its resume attempts
- [x] 8.6 Note in the `src/producer/` contract comment exactly what adding a producer costs — one config union member, one branch arm, one new file, and nothing under acceptance; verify by walking a throwaway stub through those three edits and confirming no acceptance or monitoring file needs touching
